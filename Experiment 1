#include <stdio.h>

int main() {
    int n, i;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    i = 2;
    while (i <= n) {
        printf("%d ", i);
        i = i + 2;
    }

    return 0;
}
